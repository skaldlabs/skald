import express from 'express'

export const subscriptionRouter = express.Router({ mergeParams: true })

// subscriptionRouter.get('/', list)
// subscriptionRouter.post('/', create)
// subscriptionRouter.get('/:uuid', retrieve)
// subscriptionRouter.patch('/:uuid', update)
// subscriptionRouter.delete('/:uuid', destroy)
